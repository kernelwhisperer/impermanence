{
  "name": "impermanence",
  "version": "1.0.0",
  "private": true,
  "description": "tbd",
  "dependencies": {
  },
  "scripts": {
    "clean": "git clean -fixd",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
