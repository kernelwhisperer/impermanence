{
  "name": "root",
  "version": "n/a",
  "private": true,
  "license": "UNLICENSED",
  "author": {
    "name": "Daniel Constantin",
    "email": "hello@danielconstantin.net"
  },
  "scripts": {
    "clean": "git clean -fxd",
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel start",
    "lint": "lerna run --parallel lint",
    "lint:fix": "lerna run --parallel lint:fix",
    "prebuild": "rm -rf packages/electron/web-build && lerna run build --scope=web && cp -r packages/web/build packages/electron/web-build",
    "build": "lerna run build --scope=impermanence",
    "postbuild": "lerna run make"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
